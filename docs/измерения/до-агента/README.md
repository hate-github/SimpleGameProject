# База «до»: шесть линеек на сорока зёрнах

Снято 8 сентября 2026 на коммите 8b13f65, после девяти задач плана поведения
(`docs/BEHAVIOUR_PLAN.md`) и перед фазой 0 плана агента
(`docs/AI_BEHAVIOUR_PLAN.md`). Фаза 0 поведения не меняет: проверка чтения
чужой правды и пометки причин — это комментарии и `check.py`, эталон тот же.
Зёрна 1–40, по тридцать дней. Команды:

    python замер.py --прогонов 40 > замер.txt
    python поводы.py 40 > поводы.txt
    python своё.py 40 > своё.txt
    python снаружи.py 40 > снаружи.txt
    python живость.py 40 > живость.txt
    python вдвоём.py 40 > вдвоём.txt

С этими файлами сравнивается «после» каждой фазы плана агента, которая
меняет поведение (первая такая — фаза 1); цифры до и после идут в абзац
ПЛАН.md той фазы. Прежняя база — `../до-поведения/`.

# звуки/ — записи автора

Все записи пришли из `референсы/звуки/готовые` и нарочно понижены
до 11–16 кГц: ретро здесь не в одной картинке. Имена переписаны
коротко — код зовёт их по имени без расширения (`Звуки.Взять("вьюга")`).

| файл | откуда | длина | где звучит |
|---|---|---|---|
| `вьюга.mp3` | звук вьюги 1 | 150 с | ветер, петлёй (`ЗвукUI`); громкость — от погоды дома, дома глушат стены |
| `шаги_снег_1.mp3`, `шаги_снег_2.mp3` | ходьба по снегу 1, 2 | 1.9, 1.7 с | шаг по двору и сугробам — по очереди |
| `шаги_пол.mp3` | ходьба по полу 1 | 1.2 с | шаг по бетону; по доскам — тише |
| `шаги_скрип.mp3` | скрипучий пол 1 | 1.2 с | доски своей квартиры и настил гаража — изредка, не чаще раза в четыре секунды |
| `ворота_1…3.mp3` | открытие гаражной двери 1–3 | ~2 с | створка гаража открывается и закрывается — одна из трёх наугад |
| `удар_по_металлу.mp3` | удар по металлу 1 | 1.2 с | взлом замка — удар за ударом; налёт ломится в дверь квартиры |
| `коробка.mp3` | распаковка коробки 1 | 10.8 с | обыск коробки — кусок с случайного места |
| `выключатель.mp3` | звук включения в гараже 1 | 1.5 с | выключатель в гараже |
| `лампа.mp3` | звук лампы 1 | 21.7 с | гудение лампы, петлёй: в гараже, на площадках, в подвале |
| `кипение.mp3` | закипание воды 1 | 66.5 с | на своей печи, пока она топится |
| `стекло.mp3` | разбитое стекло 1 | 2.8 с | налётчики прорвались в квартиру (исход налёта) |
| `мебель.mp3` | движение мебели 1 | 2.4 с | кто-то переезжает — у двери, куда |

## Как устроен шаг

Каждый файл шагов — **один** шаг, и удар в нём не в начале: до него
тишина (в «полу» — 0.44 с, в «снегу» — 0.40 и 0.20, в «скрипе» — 0.08).
Это снято с огибающей самих записей, а не на слух. Поэтому шаг играется
с места удара: иначе он опаздывал бы на полсекунды от ноги.

Высота шага — записанная, с разбросом в пару процентов. Больше —
и шаг звучит не живым, а замедленной или ускоренной плёнкой.

## Петли

Петлёй записи ставятся в коде (`Звуки.Взять(..., петля: true)`), а не
в настройках импорта: файлы импорта в репозиторий не входят.
